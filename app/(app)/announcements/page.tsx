import { Megaphone } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function AnnouncementsPage() {
  return <AppPage title="Announcements" icon={Megaphone} description="Broadcast important updates without losing key messages in busy chat threads." />;
}
