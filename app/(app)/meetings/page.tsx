import { Calendar } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function MeetingsPage() {
  return <AppPage title="Meetings" icon={Calendar} description="Plan agendas, capture decisions, and keep everyone aligned before and after committee meetings." />;
}
