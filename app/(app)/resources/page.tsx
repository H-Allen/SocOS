import { FolderOpen } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function ResourcesPage() {
  return <AppPage title="Resources" icon={FolderOpen} description="Keep files, links, and operational notes in one organized place for the whole committee." />;
}
