import { BookOpen } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function HandoversPage() {
  return <AppPage title="Handovers" icon={BookOpen} description="Preserve institutional memory so the next committee inherits context, not chaos." />;
}
