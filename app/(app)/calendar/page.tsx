import { CalendarDays } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function CalendarPage() {
  return <AppPage title="Calendar" icon={CalendarDays} description="See upcoming events, meetings, and important dates for your organization at a glance." />;
}
