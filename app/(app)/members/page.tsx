import { Users } from "lucide-react";

import { AppPage } from "@/components/layout/AppPage";

export default function MembersPage() {
  return <AppPage title="Members" icon={Users} description="View committee roles, permissions, and the people behind each society workspace." />;
}
